% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_roi.R
\name{set_roi}
\alias{set_roi}
\title{Crop datasets to a specified region of interest}
\usage{
set_roi(
  s2 = NULL,
  bathymetry = NULL,
  benthic = NULL,
  geomorph = NULL,
  seagrass = NULL,
  bbox = NULL
)
}
\arguments{
\item{s2}{SpatRaster. Sentinel-2 image defining the ROI. Only the spatial
extent is used. Optional, provide either \code{s2} or \code{bbox}.}

\item{bathymetry}{SpatRaster. If NULL, loads GEBCO_2024 from extdata.}

\item{benthic}{SpatRaster. If NULL, auto-loads matching Allen Coral Atlas tiles.}

\item{geomorph}{SpatRaster. If NULL, auto-loads matching Allen Coral Atlas tiles.}

\item{seagrass}{SpatVector. If NULL, loads the bundled GBR seagrass dataset.}

\item{bbox}{Numeric vector \code{c(xmin, xmax, ymin, ymax)} or an sf/sfc object
defining the ROI. Optional, alternative to \code{s2}.}
}
\value{
Named list with four elements:
\describe{
\item{bathymetry}{SpatRaster — bathymetry resampled to ACA grid (~10 m).}
\item{benthic}{SpatRaster — mosaicked benthic habitat classes (~10 m).}
\item{geomorph}{SpatRaster — mosaicked geomorphic classes (~10 m).}
\item{seagrass}{SpatVector — cropped seagrass polygons, or NULL if none
intersect the ROI.}
}
}
\description{
Crops bathymetry (GEBCO), benthic habitats, geomorphology (Allen Coral Atlas),
and seagrass datasets to the extent of a Sentinel-2 image or bounding box.
Benthic and geomorphic data are mosaicked at native ~10 m resolution;
GEBCO bathymetry is resampled onto the ACA grid.
}
\examples{
bbox <- c(150.56, 151.30, -21.51, -20.77)
result <- set_roi(bbox = bbox)
plot(result$benthic)

\dontrun{
s2_img <- terra::rast(system.file("extdata", "Sentinel2_2025-08-28_AllBands.tif",
                                   package = "ReefMappeR"))
result  <- set_roi(s2 = s2_img)
plot(result$benthic)
}
}
